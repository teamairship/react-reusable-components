
import React from 'react';

interface Props {
  isShowing?: boolean,
}
const NavDrawer: React.FC<Props> = ({
  isShowing,
}) => {
  return (
    <div>
      nav drawah
    </div>
  );
};

export default NavDrawer;
